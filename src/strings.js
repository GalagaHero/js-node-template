const name = "Alex Frasca";
const course = "CIS-177 Web Development";
const grade = 85;

const trad =
	name + " is taking " + course + " and expects to earn " + grade + "%.";
const temp = `${name} is taking ${course} and expects to earn ${grade}%.`;

console.log(trad);
console.log(temp);
